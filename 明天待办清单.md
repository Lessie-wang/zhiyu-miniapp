# 知愈小程序 - 明天待办清单 📋

## 🎯 第一阶段：云开发环境配置（预计30分钟）

### ✅ 任务1：开通微信云开发（10分钟）

- [X] 打开微信开发者工具 ✅ 2026-02-19
- [X] 点击顶部菜单栏的 **"云开发"** 按钮 ✅ 2026-02-19
- [X] 点击 **"开通"** 按钮 ✅ 2026-02-19
- [X] 填写环境名称（建议：`zhiyu-prod`） ✅ 2026-02-19
- [X] 等待环境创建完成（约1-2分钟） ✅ 2026-02-19
- [X] 进入云开发控制台，点击 **"设置"** 标签 ✅ 2026-02-19
- [X] 复制 **环境ID**（格式如：zhiyu-xxxxx） ✅ 2026-02-19
- [X] 打开 `app.js` 文件，第13行，将 `'your-env-id'` 替换为你的环境ID ✅ 2026-02-19

### ✅ 任务2：创建数据库集合（10分钟）

在云开发控制台 → 数据库 → 添加集合：

- [X] 创建集合：`emotion_records`（情绪记录表） ✅ 2026-02-19
- [X] 创建集合：`user_stats`（用户统计表） ✅ 2026-02-19
- [X] 创建集合：`ai_conversations`（AI对话记录，可选） ✅ 2026-02-19
- [X] 创建集合：`period_records`（经期记录，可选） ✅ 2026-02-19

**配置权限（重要）：**

- [X] 点击每个集合 → 权限设置 → 选择 **"自定义安全规则"** ✅ 2026-02-19
- [X] 粘贴以下规则： ✅ 2026-02-19

```json
{
  "read": "doc.openid == auth.openid",
  "write": "doc.openid == auth.openid"
}
```

### ✅ 任务3：上传云函数（10分钟）

在微信开发者工具中，右键点击以下文件夹，选择 **"上传并部署：云端安装依赖"**：

- [X] `cloudfunctions/saveEmotion` → 上传并部署 ✅ 2026-02-19
- [X] `cloudfunctions/getEmotions` → 上传并部署 ✅ 2026-02-19
- [X] `cloudfunctions/getEmotionStats` → 上传并部署 ✅ 2026-02-19

**验证上传成功：**

- [X] 在云开发控制台 → 云函数，查看是否有3个云函数 ✅ 2026-02-19
- [X] 点击任意云函数 → 测试，输入测试数据验证

---

## 🎯 第二阶段：测试云开发功能（预计20分钟）

### ✅ 任务4：测试云函数（10分钟）

**测试 saveEmotion：**

- [X] 云开发控制台 → 云函数 → saveEmotion → 测试 ✅ 2026-02-19
- [X] 输入测试数据： ✅ 2026-02-19

```json
{
  "emotion": "开心",
  "emotionIcon": "😊",
  "intensity": 8,
  "note": "测试记录",
  "recordDate": "2024-02-18T10:00:00.000Z"
}
```

- [X] 点击 **"运行测试"**，查看返回结果 ✅ 2026-02-19
- [X] 在数据库 → emotion_records 中查看是否有新记录 ✅ 2026-02-19

**测试 getEmotions：**

- [ ] 云函数 → getEmotions → 测试
- [ ] 输入：`{ "limit": 10 }`
- [ ] 查看是否返回刚才保存的记录

**测试 getEmotionStats：**

- [ ] 云函数 → getEmotionStats → 测试
- [ ] 输入：`{ "timeRange": "all" }`
- [ ] 查看统计数据是否正确

### ✅ 任务5：在小程序中测试（10分钟）

- [X] 在微信开发者工具中，点击 **"编译"**
- [X] 打开小程序，进入情绪记录页面
- [X] 记录一次情绪（目前还是保存到本地）
- [X] 打开控制台，输入以下代码测试云函数：

```javascript
const cloudUtil = require('./utils/cloud.js');

// 测试保存
cloudUtil.saveEmotionToCloud({
  emotion: '开心',
  emotionIcon: '😊',
  intensity: 8,
  note: '小程序测试',
  recordDate: new Date().toISOString()
}).then(id => {
  console.log('保存成功，ID:', id);
});

// 测试获取
cloudUtil.getEmotionsFromCloud({ limit: 10 }).then(result => {
  console.log('记录列表:', result.records);
});
```

---

## 🎯 第三阶段：集成云开发到小程序（预计30分钟）

### ✅ 任务6：更新保存记录功能（15分钟）

**需要修改的文件：**

- [ ] `pages/main/main.js` - 情绪记录页面
- [ ] `pages/record/record.js` - 快速记录页面

**修改步骤：**

1. 在文件顶部引入云工具：

```javascript
const cloudUtil = require('../../utils/cloud.js');
```

2. 找到保存记录的函数（通常是 `saveRecord`），修改为：

```javascript
async saveRecord() {
  // 原有的本地保存代码...

  // 新增：同时保存到云端
  try {
    await cloudUtil.saveEmotionToCloud({
      emotion: this.data.selectedEmotion,
      emotionIcon: this.getEmotionIcon(this.data.selectedEmotion),
      intensity: this.data.intensity,
      note: this.data.noteText,
      bodyFeelings: this.data.selectedBodyFeelings,
      recordDate: new Date().toISOString()
    });
    console.log('云端保存成功');
  } catch (error) {
    console.error('云端保存失败，已保存到本地', error);
  }
}
```

### ✅ 任务7：更新历史页面加载数据（15分钟）

**修改文件：** `pages/history/history.js`

1. 引入云工具：

```javascript
const cloudUtil = require('../../utils/cloud.js');
```

2. 修改 `loadRecords` 函数：

```javascript
async loadRecords() {
  try {
    // 从云端获取记录
    const result = await cloudUtil.getEmotionsFromCloud({
      limit: 100
    });

    this.setData({
      records: result.records,
      totalRecords: result.total
    });

    // 按日期分组
    this.groupRecordsByDate(result.records);
  } catch (error) {
    console.error('加载云端数据失败，使用本地数据', error);
    // 降级到本地数据
    const localRecords = wx.getStorageSync('emotionRecords') || [];
    this.setData({ records: localRecords });
  }
}
```

3. 修改 `calculateStats` 函数：

```javascript
async calculateStats() {
  try {
    const stats = await cloudUtil.getEmotionStatsFromCloud(this.data.timeRange);

    this.setData({
      emotionStats: stats.emotionStats,
      aiSummary: stats.aiSummary,
      'userInfo.useDays': stats.useDays,
      'userInfo.recordDays': stats.recordDays
    });
  } catch (error) {
    console.error('获取统计数据失败', error);
  }
}
```

---

## 🎯 第四阶段：数据迁移（可选，预计10分钟）

### ✅ 任务8：同步本地数据到云端（10分钟）

如果你之前有本地测试数据，可以同步到云端：

- [ ] 在小程序控制台执行：

```javascript
const cloudUtil = require('./utils/cloud.js');

cloudUtil.syncLocalDataToCloud().then(success => {
  console.log('同步完成:', success);
});
```

- [ ] 在云开发控制台 → 数据库 → emotion_records 中查看数据

---

## 🎯 第五阶段：测试完整流程（预计15分钟）

### ✅ 任务9：端到端测试（15分钟）

- [ ] **测试1：记录情绪**

  - 打开小程序，记录一次情绪
  - 查看云数据库是否有新记录
- [ ] **测试2：查看历史**

  - 进入"我的历史"页面
  - 查看是否显示云端数据
  - 点击日历日期，查看详情
- [ ] **测试3：查看统计**

  - 切换时间范围（本周/本月/全部）
  - 查看情绪统计图表
  - 查看AI摘要
- [ ] **测试4：多设备同步**

  - 在另一台设备登录同一个微信
  - 查看数据是否同步

---

## 📝 完成后的检查清单

- [X] 云开发环境已开通，环境ID已配置 ✅ 2026-02-19
- [X] 数据库集合已创建，权限已配置 ✅ 2026-02-19
- [X] 3个云函数已上传并测试通过
- [X] 小程序可以保存记录到云端
- [ ] 小程序可以从云端加载记录
- [ ] 统计功能正常工作
- [ ] 数据在多设备间同步

---

## 🆘 遇到问题怎么办？

### 常见问题排查：

**问题1：云函数调用失败**

- 检查 app.js 中的环境ID是否正确
- 检查云函数是否已上传
- 查看云函数日志（云开发控制台 → 云函数 → 日志）

**问题2：数据库写入失败**

- 检查数据库集合是否已创建
- 检查权限设置是否正确
- 查看云函数日志

**问题3：小程序报错**

- 打开调试器查看错误信息
- 检查 utils/cloud.js 是否正确引入
- 确认基础库版本 ≥ 2.2.3

**问题4：数据不同步**

- 确认使用的是同一个微信账号
- 检查网络连接
- 查看云函数调用日志

---

## 💡 小贴士

1. **先测试再集成**：先在云开发控制台测试云函数，确认无误后再集成到小程序
2. **保留本地存储**：建议保留本地存储作为降级方案，网络失败时仍可使用
3. **查看日志**：遇到问题先查看云函数日志，90%的问题都能找到原因
4. **分步测试**：每完成一个任务就测试一次，不要等全部完成再测试
5. **备份数据**：在数据库中导出数据作为备份

---

## 🎉 完成后的成果

完成以上所有任务后，你将拥有：

✅ 完整的云开发环境
✅ 用户数据云端存储
✅ 多设备数据同步
✅ AI情绪分析功能
✅ 完整的统计报告
✅ 可扩展的后端架构

**预计总耗时：约 1.5 - 2 小时**

---

## 📚 参考文档

- [云开发配置指南.md](./云开发配置指南.md) - 详细配置步骤
- [README_云开发.md](./README_云开发.md) - 快速开始指南
- [微信云开发官方文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)

---

**加油！明天完成这些任务后，知愈小程序就有完整的后端支持了！** 🚀

有问题随时问我！
