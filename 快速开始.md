# 🚀 快速开始 - 5分钟速查卡

## 📋 明天的核心任务（按优先级）

### 🔥 必做任务（30分钟）

#### 1️⃣ 开通云开发（10分钟）
```
微信开发者工具 → 云开发 → 开通 → 复制环境ID
↓
打开 app.js 第13行 → 粘贴环境ID
```

#### 2️⃣ 创建数据库（10分钟）
```
云开发控制台 → 数据库 → 添加集合
↓
创建：emotion_records、user_stats
↓
权限设置 → 自定义规则 → 粘贴权限代码
```

#### 3️⃣ 上传云函数（10分钟）
```
右键 cloudfunctions/saveEmotion → 上传并部署
右键 cloudfunctions/getEmotions → 上传并部署
右键 cloudfunctions/getEmotionStats → 上传并部署
```

---

## 🎯 测试验证（15分钟）

### 测试云函数
```javascript
// 在云开发控制台 → 云函数 → saveEmotion → 测试
{
  "emotion": "开心",
  "emotionIcon": "😊",
  "intensity": 8,
  "note": "测试",
  "recordDate": "2024-02-18T10:00:00.000Z"
}
```

### 测试小程序
```javascript
// 在小程序控制台输入
const cloudUtil = require('./utils/cloud.js');
cloudUtil.saveEmotionToCloud({
  emotion: '开心',
  emotionIcon: '😊',
  intensity: 8,
  note: '测试',
  recordDate: new Date().toISOString()
}).then(id => console.log('成功:', id));
```

---

## 📝 权限配置代码（复制粘贴）

```json
{
  "read": "doc.openid == auth.openid",
  "write": "doc.openid == auth.openid"
}
```

---

## 🔧 需要修改的文件（可选）

### 1. pages/main/main.js
```javascript
// 文件顶部添加
const cloudUtil = require('../../utils/cloud.js');

// 在 saveRecord 函数中添加
try {
  await cloudUtil.saveEmotionToCloud({
    emotion: this.data.selectedEmotion,
    emotionIcon: '😊', // 根据实际情绪获取
    intensity: 8,
    note: this.data.noteText,
    recordDate: new Date().toISOString()
  });
} catch (error) {
  console.error('云端保存失败', error);
}
```

### 2. pages/history/history.js
```javascript
// 文件顶部添加
const cloudUtil = require('../../utils/cloud.js');

// 修改 loadRecords 函数
async loadRecords() {
  const result = await cloudUtil.getEmotionsFromCloud({ limit: 100 });
  this.setData({ records: result.records });
}

// 修改 calculateStats 函数
async calculateStats() {
  const stats = await cloudUtil.getEmotionStatsFromCloud(this.data.timeRange);
  this.setData({ emotionStats: stats.emotionStats });
}
```

---

## ⚠️ 常见错误速查

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 云函数调用失败 | 环境ID错误 | 检查 app.js 第13行 |
| 数据库写入失败 | 权限未配置 | 检查数据库权限设置 |
| 云函数未找到 | 未上传 | 右键上传云函数 |
| openid 为空 | 未登录 | 检查云开发初始化 |

---

## 📞 遇到问题？

1. **查看云函数日志**：云开发控制台 → 云函数 → 日志
2. **查看小程序控制台**：微信开发者工具 → 调试器 → Console
3. **参考文档**：
   - [云开发配置指南.md](./云开发配置指南.md)
   - [明天待办清单.md](./明天待办清单.md)

---

## ✅ 完成标志

- [ ] 云开发控制台能看到3个云函数
- [ ] 数据库有2个集合（emotion_records、user_stats）
- [ ] 小程序能成功保存记录到云端
- [ ] 云数据库中能看到新记录

---

## 🎉 完成后

恭喜！你的知愈小程序已经有完整的后端支持了！

**下一步：**
- 测试多设备同步
- 优化用户体验
- 准备小程序审核

---

**预计耗时：45分钟 - 1小时**

加油！🚀
