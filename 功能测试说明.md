# 知愈小程序 - 功能测试说明

## 📱 主要功能测试指南

### 1. 情绪觉察页面测试

**页面路径**：`pages/emotion/emotion`

#### 测试步骤：
1. ✅ **查看 8 种核心情绪**
   - 应该看到 2x4 网格布局
   - 每个情绪卡片显示：图标、名称、定义、衍生数量

2. ✅ **点击情绪卡片**
   - 卡片应该高亮显示（渐变背景）
   - 下方展示该情绪的 8 个衍生情绪
   - 有触觉反馈（震动）

3. ✅ **查看衍生情绪**
   - 标题显示"XX的衍生情绪"
   - 描述显示"这些都是XX的不同表现形式"
   - 衍生情绪以标签形式展示

4. ✅ **底部按钮**
   - "开始情绪记录"按钮 → 跳转到首页
   - "了解表达训练方法"按钮 → 显示"功能开发中"

---

### 2. 主页情绪记录功能测试

**页面路径**：`pages/main/main`

#### 测试步骤：

##### 步骤 1：选择情绪
1. 横向滑动查看 8 种情绪
2. 点击一个或多个情绪
3. ✅ **预期结果**：
   - 选中的情绪卡片显示 ✓ 标记
   - 卡片背景变为渐变色
   - 有触觉反馈

##### 步骤 2：选择身体感受（可选）
1. 选择情绪后，下方出现"身体有什么感觉吗？"区域
2. 提示文字："可选，帮助 AI 更好地理解你"
3. 点击一个或多个身体感受标签
4. ✅ **预期结果**：
   - 选中的标签背景变为渐变色
   - 可以多选
   - 可以取消选择
   - 有触觉反馈

##### 步骤 3：开始 AI 引导
1. 点击"开始 AI 引导"按钮（🤖 图标）
2. ✅ **预期结果**：
   - 按钮消失
   - 显示用户的情绪和身体感受消息
   - 显示"AI 正在思考"动画（三个跳动的点）
   - AI 回复出现在对话区

##### 步骤 4：与 AI 对话
1. 在输入框输入文字
2. 点击发送按钮（→）或按回车
3. ✅ **预期结果**：
   - 用户消息显示在右侧
   - AI 消息显示在左侧
   - 显示"AI 正在思考"动画
   - AI 回复基于用户画像个性化

##### 步骤 5：保存记录
1. 点击"保存今天的记录"按钮
2. ✅ **预期结果**：
   - 显示"记录已保存"提示
   - 2 秒后页面重置
   - 记录保存到本地存储

---

### 3. AI 功能测试

#### 场景 1：API 调用成功
**前提条件**：已配置合法域名 `https://api.deepseek.com`

1. 选择情绪 → 点击"开始 AI 引导"
2. ✅ **预期结果**：
   - 显示"AI 正在思考"
   - 3-5 秒后收到 DeepSeek API 的回复
   - 回复内容基于用户画像（性别、职业、困扰、目标）
   - 回复简短温暖（50 字以内）

#### 场景 2：API 调用失败（降级模式）
**前提条件**：未配置合法域名或 API Key 无效

1. 选择情绪 → 点击"开始 AI 引导"
2. ✅ **预期结果**：
   - 显示"AI 正在思考"
   - 显示提示"AI 暂时无法回复"
   - 自动降级到本地模式
   - 显示本地预设的引导问题

---

## 🐛 问题排查

### 问题 1：情绪页面显示空白或数据不显示

**可能原因**：
- 页面未正确加载数据
- WXML 数据绑定错误

**排查方法**：
1. 打开微信开发者工具控制台
2. 查看是否有 JavaScript 错误
3. 在 `emotion.js` 的 `onLoad` 中添加 `console.log(this.data.emotions)` 查看数据

**解决方法**：
```javascript
// 在 emotion.js 的 onLoad 中
onLoad: function(options) {
  const emotionsArray = Object.keys(emotionsData).map(key => emotionsData[key]);
  console.log('情绪数据:', emotionsArray); // 调试用
  this.setData({
    emotions: emotionsArray
  });
}
```

---

### 问题 2：身体感受无法选择

**可能原因**：
- 数据绑定错误
- `selectedBodyFeelings` 数组未初始化

**排查方法**：
1. 检查 `main.js` 中 `data` 是否包含 `selectedBodyFeelings: []`
2. 检查 `toggleBodyFeeling` 函数是否正确执行
3. 在控制台查看点击事件是否触发

**解决方法**：
```javascript
// 确保 data 中有初始化
data: {
  selectedBodyFeelings: [], // 必须初始化为空数组
  // ...
}
```

---

### 问题 3：AI 无法回复

**可能原因**：
1. 未配置微信小程序合法域名
2. API Key 无效或额度用完
3. 网络问题

**排查方法**：
1. 查看控制台错误信息
2. 检查是否显示"AI 暂时无法回复"提示
3. 查看网络请求是否发送成功

**解决方法**：
- **开发阶段**：在开发工具中勾选"不校验合法域名"
- **正式发布**：必须在微信小程序后台配置 `https://api.deepseek.com`

详细配置步骤请查看：[API配置说明.md](./API配置说明.md)

---

## ✅ 功能检查清单

### 情绪觉察页面
- [ ] 8 种情绪以 2x4 网格显示
- [ ] 每个情绪显示图标、名称、定义、衍生数量
- [ ] 点击情绪卡片后高亮
- [ ] 下方显示 8 个衍生情绪
- [ ] 底部按钮可以点击

### 主页情绪记录
- [ ] 可以选择多个情绪
- [ ] 选中的情绪显示 ✓ 标记
- [ ] 身体感受可以多选
- [ ] 选中的身体感受标签高亮
- [ ] "开始 AI 引导"按钮显示
- [ ] 点击按钮后触发 AI 对话

### AI 对话功能
- [ ] 显示"AI 正在思考"动画
- [ ] AI 回复出现在对话区
- [ ] 可以继续输入并发送消息
- [ ] API 失败时自动降级到本地模式
- [ ] 显示错误提示

### 保存功能
- [ ] 可以保存记录
- [ ] 显示"记录已保存"提示
- [ ] 页面自动重置

---

## 📊 测试数据

### 用户画像示例
```json
{
  "gender": "女性",
  "occupation": "职场人士",
  "concerns": ["不知道如何表达情绪", "害怕表达后被误解"],
  "goals": ["提升情绪表达能力", "改善人际关系"]
}
```

### 情绪数据
- 8 种核心情绪：喜悦、信任、恐惧、惊讶、悲伤、厌恶、愤怒、期待
- 每种情绪有 8 个衍生情绪

### 身体感受选项
- 12 种：心跳加速、呼吸急促、胸口发闷、肩膀紧绷、手心出汗、胃部不适、头晕目眩、浑身无力、肌肉紧张、喉咙发紧、身体放松、精力充沛

---

## 🔍 调试技巧

### 1. 查看数据
在 JS 文件中添加 `console.log` 查看数据：
```javascript
console.log('当前情绪:', this.data.selectedEmotions);
console.log('身体感受:', this.data.selectedBodyFeelings);
console.log('对话历史:', this.data.messages);
```

### 2. 查看网络请求
在微信开发者工具中：
1. 点击「Network」标签
2. 查看 API 请求和响应
3. 检查状态码和返回数据

### 3. 查看存储数据
在控制台输入：
```javascript
console.log(wx.getStorageSync('userProfile'));
console.log(wx.getStorageSync('dailyRecords'));
```

---

## 📞 需要帮助？

如果测试过程中遇到问题：
1. 查看控制台的详细错误信息
2. 参考 [API配置说明.md](./API配置说明.md)
3. 检查代码中的数据绑定是否正确
4. 确认微信开发者工具版本是最新的
