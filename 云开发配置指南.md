# 微信云开发配置指南

## 一、开通云开发环境

### 1. 在微信开发者工具中开通
1. 打开微信开发者工具
2. 点击顶部菜单 "云开发" 按钮
3. 点击 "开通" 按钮
4. 选择环境名称（如：zhiyu-prod）
5. 等待环境创建完成（约1-2分钟）

### 2. 获取环境ID
1. 在云开发控制台，点击 "设置" 标签
2. 复制 "环境ID"（格式如：zhiyu-xxxxx）
3. 将环境ID填入 `app.js` 的第13行：
   ```javascript
   env: 'your-env-id', // 替换为你的环境ID
   ```

## 二、创建数据库集合

在云开发控制台 -> 数据库 -> 添加集合，创建以下集合：

### 1. emotion_records（情绪记录表）
```json
{
  "_id": "自动生成",
  "openid": "用户openid",
  "emotion": "开心",
  "emotionIcon": "😊",
  "intensity": 8,
  "note": "今天很开心",
  "bodyFeelings": ["放松", "精力充沛"],
  "recordDate": "2024-02-18T10:30:00.000Z",
  "createdAt": "2024-02-18T10:30:00.000Z"
}
```

**索引设置：**
- 字段：openid, recordDate（组合索引）
- 字段：openid, emotion（组合索引）

### 2. user_stats（用户统计表）
```json
{
  "_id": "自动生成",
  "openid": "用户openid",
  "recordDates": ["2024-02-18", "2024-02-19"],
  "firstUseDate": "2024-02-18T10:00:00.000Z",
  "createdAt": "2024-02-18T10:00:00.000Z"
}
```

**索引设置：**
- 字段：openid（唯一索引）

### 3. ai_conversations（AI对话记录表）
```json
{
  "_id": "自动生成",
  "openid": "用户openid",
  "emotionRecordId": "关联的情绪记录ID",
  "role": "user",
  "content": "我今天很开心",
  "createdAt": "2024-02-18T10:30:00.000Z"
}
```

### 4. period_records（经期记录表）
```json
{
  "_id": "自动生成",
  "openid": "用户openid",
  "startDate": "2024-02-18",
  "endDate": "2024-02-23",
  "cycleLength": 28,
  "painLevel": 5,
  "symptoms": ["腹痛", "疲劳"],
  "createdAt": "2024-02-18T10:00:00.000Z"
}
```

## 三、上传云函数

### 1. 在微信开发者工具中上传
1. 右键点击 `cloudfunctions/saveEmotion` 文件夹
2. 选择 "上传并部署：云端安装依赖"
3. 等待上传完成

重复以上步骤，上传以下云函数：
- `saveEmotion` - 保存情绪记录
- `getEmotions` - 获取情绪记录列表
- `getEmotionStats` - 获取统计数据

### 2. 验证云函数
在云开发控制台 -> 云函数 -> 点击函数名 -> 测试，输入测试数据验证。

## 四、配置数据库权限

在云开发控制台 -> 数据库 -> 集合 -> 权限设置：

### 推荐权限配置
```json
{
  "read": "doc.openid == auth.openid",
  "write": "doc.openid == auth.openid"
}
```

这样配置后，用户只能读写自己的数据。

## 五、小程序端调用示例

### 1. 保存情绪记录
```javascript
const cloudUtil = require('../../utils/cloud.js');

// 保存记录
const recordId = await cloudUtil.saveEmotionToCloud({
  emotion: '开心',
  emotionIcon: '😊',
  intensity: 8,
  note: '今天很开心',
  bodyFeelings: ['放松', '精力充沛'],
  recordDate: new Date().toISOString()
});
```

### 2. 获取记录列表
```javascript
// 获取最近30天的记录
const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);

const result = await cloudUtil.getEmotionsFromCloud({
  startDate: thirtyDaysAgo.toISOString(),
  endDate: new Date().toISOString(),
  limit: 50
});

console.log('记录列表:', result.records);
console.log('总数:', result.total);
```

### 3. 获取统计数据
```javascript
// 获取本周统计
const stats = await cloudUtil.getEmotionStatsFromCloud('week');

console.log('总记录数:', stats.totalRecords);
console.log('情绪统计:', stats.emotionStats);
console.log('使用天数:', stats.useDays);
console.log('AI摘要:', stats.aiSummary);
```

## 六、数据同步策略

### 方案A：仅云端存储（推荐）
- 所有数据直接保存到云端
- 优点：数据安全、多端同步
- 缺点：需要网络连接

### 方案B：本地+云端双存储
- 数据同时保存到本地和云端
- 本地作为缓存，提升加载速度
- 定期同步本地数据到云端

```javascript
// 保存时同时存储
async function saveEmotion(data) {
  // 1. 保存到本地
  const localRecords = wx.getStorageSync('emotionRecords') || [];
  localRecords.push(data);
  wx.setStorageSync('emotionRecords', localRecords);

  // 2. 保存到云端
  try {
    await cloudUtil.saveEmotionToCloud(data);
  } catch (error) {
    console.error('云端保存失败，已保存到本地');
  }
}
```

## 七、费用说明

### 免费额度（每月）
- 数据库：5GB 存储 + 5GB 读写
- 云函数：10万次调用 + 4万GBs资源使用量
- 云存储：5GB 容量 + 10GB CDN流量

### 预估使用量
- 1000个活跃用户
- 每人每天记录3次情绪
- 每月约：9万次云函数调用
- 数据库存储：约50MB

**结论：免费额度完全够用！**

## 八、常见问题

### Q1: 云函数调用失败？
A: 检查以下几点：
1. 是否已上传云函数
2. app.js 中的环境ID是否正确
3. 云函数是否有语法错误（查看云函数日志）

### Q2: 数据库写入失败？
A: 检查：
1. 数据库集合是否已创建
2. 权限设置是否正确
3. 数据格式是否符合要求

### Q3: 如何查看云函数日志？
A: 云开发控制台 -> 云函数 -> 点击函数名 -> 日志

### Q4: 如何备份数据？
A: 云开发控制台 -> 数据库 -> 导出 -> 选择集合 -> 导出为JSON

## 九、下一步

1. ✅ 配置云开发环境
2. ✅ 创建数据库集合
3. ✅ 上传云函数
4. ⏳ 更新小程序页面调用云函数
5. ⏳ 测试数据保存和读取
6. ⏳ 发布小程序

需要帮助请参考：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html
