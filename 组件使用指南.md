# 知愈小程序组件使用指南

## 目录

1. [设计系统](#设计系统)
2. [按钮组件 (zh-button)](#按钮组件)
3. [卡片组件 (card)](#卡片组件)
4. [情绪标签组件 (emotion-tag)](#情绪标签组件)
5. [加载组件 (zh-loading)](#加载组件)
6. [最佳实践](#最佳实践)

---

## 设计系统

### 引入设计系统

在页面的 `.wxss` 文件中引入设计系统：

```css
@import "../../styles/design-tokens.wxss";
@import "../../styles/animations.wxss";
```

### 设计令牌使用

#### 颜色

```css
/* 主色调 */
color: var(--color-primary);           /* #D4B8A5 */
color: var(--color-primary-dark);      /* #C4A893 */
color: var(--color-primary-light);     /* #E8D5C4 */

/* 文字颜色 */
color: var(--color-text-primary);      /* #6D6A65 */
color: var(--color-text-secondary);    /* #8C8780 */
color: var(--color-text-tertiary);     /* #B8B2A7 */

/* 背景颜色 */
background: var(--color-bg-primary);   /* #FAF9F7 */
background: var(--color-bg-secondary); /* #F5F0EB */
background: var(--color-bg-card);      /* #FFFFFF */
```

#### 间距

```css
padding: var(--spacing-xs);    /* 8rpx */
padding: var(--spacing-sm);    /* 12rpx */
padding: var(--spacing-md);    /* 16rpx */
padding: var(--spacing-lg);    /* 24rpx */
padding: var(--spacing-xl);    /* 32rpx */
padding: var(--spacing-2xl);   /* 40rpx */
padding: var(--spacing-3xl);   /* 48rpx */
padding: var(--spacing-4xl);   /* 64rpx */
```

#### 圆角

```css
border-radius: var(--radius-sm);    /* 12rpx */
border-radius: var(--radius-md);    /* 16rpx */
border-radius: var(--radius-lg);    /* 20rpx */
border-radius: var(--radius-xl);    /* 24rpx */
border-radius: var(--radius-2xl);   /* 32rpx */
border-radius: var(--radius-full);  /* 9999rpx (圆形) */
```

#### 阴影

```css
box-shadow: var(--shadow-sm);   /* 轻微阴影 */
box-shadow: var(--shadow-md);   /* 中等阴影 */
box-shadow: var(--shadow-lg);   /* 较大阴影 */
box-shadow: var(--shadow-xl);   /* 超大阴影 */
```

#### 动画

```css
/* 在元素上添加动画类 */
animation: fadeIn var(--transition-normal) var(--ease-smooth);
animation: fadeInUp var(--transition-normal) var(--ease-smooth);
animation: breathe 4s var(--ease-smooth) infinite;
```

---

## 按钮组件

### 基本用法

#### 1. 在页面 JSON 中注册组件

```json
{
  "usingComponents": {
    "zh-button": "/components/button/button"
  }
}
```

#### 2. 在 WXML 中使用

```xml
<zh-button type="primary" bind:tap="handleClick">
  保存记录
</zh-button>
```

### 属性说明

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | String | 'primary' | 按钮类型: primary / secondary / ghost / text / glass |
| size | String | 'medium' | 按钮尺寸: small / medium / large |
| disabled | Boolean | false | 是否禁用 |
| loading | Boolean | false | 是否显示加载状态 |
| block | Boolean | false | 是否为块级按钮(宽度100%) |
| icon | String | '' | 图标路径 |
| customClass | String | '' | 自定义样式类 |

### 事件

| 事件名 | 说明 | 参数 |
|--------|------|------|
| tap | 点击按钮时触发 | event |

### 示例

#### 主要按钮

```xml
<zh-button type="primary" size="large" bind:tap="onSave">
  保存
</zh-button>
```

#### 次要按钮

```xml
<zh-button type="secondary" bind:tap="onCancel">
  取消
</zh-button>
```

#### 幽灵按钮

```xml
<zh-button type="ghost" bind:tap="onEdit">
  编辑
</zh-button>
```

#### 文字按钮

```xml
<zh-button type="text" bind:tap="onMore">
  查看更多
</zh-button>
```

#### 毛玻璃按钮

```xml
<zh-button type="glass" bind:tap="onClick">
  毛玻璃效果
</zh-button>
```

#### 带图标的按钮

```xml
<zh-button 
  type="primary" 
  icon="/images/icons/add.png"
  bind:tap="onAdd">
  添加记录
</zh-button>
```

#### 加载状态按钮

```xml
<zh-button 
  type="primary" 
  loading="{{isLoading}}"
  disabled="{{isLoading}}"
  bind:tap="onSubmit">
  提交中...
</zh-button>
```

#### 块级按钮

```xml
<zh-button type="primary" block="{{true}}" bind:tap="onConfirm">
  确认
</zh-button>
```

#### 小尺寸按钮

```xml
<zh-button type="primary" size="small" bind:tap="onClick">
  小按钮
</zh-button>
```

---

## 卡片组件

### 基本用法

#### 1. 在页面 JSON 中注册组件

```json
{
  "usingComponents": {
    "card": "/components/card/card"
  }
}
```

#### 2. 在 WXML 中使用

```xml
<card title="情绪记录">
  <view>这是卡片内容</view>
</card>
```

### 属性说明

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | String | 'default' | 卡片类型: default / glass / elevated |
| title | String | '' | 卡片标题 |
| extra | String | '' | 标题右侧额外内容 |
| shadow | Boolean | true | 是否显示阴影 |
| hoverable | Boolean | false | 是否启用悬停效果 |
| customClass | String | '' | 自定义样式类 |

### 插槽

| 插槽名 | 说明 |
|--------|------|
| 默认插槽 | 卡片主体内容 |
| header | 自定义头部内容 |
| footer | 自定义底部内容 |

### 事件

| 事件名 | 说明 | 参数 |
|--------|------|------|
| tap | 点击卡片时触发 | event |

### 示例

#### 基础卡片

```xml
<card title="今日情绪">
  <view class="content">
    <text>开心、平静</text>
  </view>
</card>
```

#### 毛玻璃卡片

```xml
<card type="glass" title="情绪轮盘">
  <view class="emotion-wheel">
    <!-- 情绪轮盘内容 -->
  </view>
</card>
```

#### 悬浮卡片

```xml
<card type="elevated" shadow="{{true}}" hoverable="{{true}}">
  <view class="card-content">
    <text>这是一个悬浮卡片</text>
  </view>
</card>
```

#### 带额外内容的卡片

```xml
<card title="历史记录" extra="查看全部">
  <view class="history-list">
    <!-- 历史记录列表 -->
  </view>
</card>
```

#### 自定义头部和底部

```xml
<card>
  <view slot="header">
    <view class="custom-header">
      <text>自定义头部</text>
      <button>操作</button>
    </view>
  </view>
  
  <view class="card-body">
    卡片主体内容
  </view>
  
  <view slot="footer">
    <view class="custom-footer">
      <button>取消</button>
      <button>确认</button>
    </view>
  </view>
</card>
```

#### 可点击的卡片

```xml
<card 
  title="情绪详情" 
  hoverable="{{true}}"
  bind:tap="onCardClick">
  <view class="emotion-detail">
    <!-- 情绪详情内容 -->
  </view>
</card>
```

---

## 情绪标签组件

### 基本用法

#### 1. 在页面 JSON 中注册组件

```json
{
  "usingComponents": {
    "emotion-tag": "/components/emotion-tag/emotion-tag"
  }
}
```

#### 2. 在 WXML 中使用

```xml
<emotion-tag 
  name="开心" 
  icon="/images/emotion-icons/happy.png"
  bg-color="rgba(255, 212, 216, 0.9)"
/>
```

### 属性说明

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| name | String | '' | 标签名称 |
| icon | String | '' | 图标路径 |
| bgColor | String | 'rgba(212, 184, 165, 0.15)' | 背景颜色 |
| textColor | String | '#6D6A65' | 文字颜色 |
| size | String | 'medium' | 尺寸: small / medium / large |
| clickable | Boolean | false | 是否可点击 |
| closable | Boolean | false | 是否显示关闭按钮 |
| selected | Boolean | false | 是否选中状态 |
| customClass | String | '' | 自定义样式类 |

### 事件

| 事件名 | 说明 | 参数 |
|--------|------|------|
| tap | 点击标签时触发 | event |
| close | 点击关闭按钮时触发 | event |

### 示例

#### 基础标签

```xml
<emotion-tag name="开心" />
```

#### 带图标的标签

```xml
<emotion-tag 
  name="开心" 
  icon="/images/emotion-icons/happy.png"
  bg-color="rgba(255, 212, 216, 0.9)"
/>
```

#### 可点击的标签

```xml
<emotion-tag 
  name="平静" 
  icon="/images/emotion-icons/calm.png"
  bg-color="rgba(173, 216, 230, 0.9)"
  clickable="{{true}}"
  bind:tap="onTagClick"
/>
```

#### 可关闭的标签

```xml
<emotion-tag 
  name="焦虑" 
  icon="/images/emotion-icons/anxious.png"
  bg-color="rgba(255, 228, 181, 0.9)"
  closable="{{true}}"
  bind:close="onTagClose"
/>
```

#### 选中状态的标签

```xml
<emotion-tag 
  name="开心" 
  icon="/images/emotion-icons/happy.png"
  bg-color="rgba(255, 212, 216, 0.9)"
  selected="{{true}}"
  clickable="{{true}}"
  bind:tap="onTagClick"
/>
```

#### 不同尺寸的标签

```xml
<!-- 小尺寸 -->
<emotion-tag name="开心" size="small" />

<!-- 中等尺寸 (默认) -->
<emotion-tag name="开心" size="medium" />

<!-- 大尺寸 -->
<emotion-tag name="开心" size="large" />
```

#### 自定义颜色的标签

```xml
<emotion-tag 
  name="自定义" 
  bg-color="rgba(200, 150, 200, 0.9)"
  text-color="#FFFFFF"
/>
```

#### 标签列表

```xml
<view class="tag-list">
  <emotion-tag 
    wx:for="{{emotions}}" 
    wx:key="id"
    name="{{item.name}}" 
    icon="{{item.icon}}"
    bg-color="{{item.bgColor}}"
    clickable="{{true}}"
    bind:tap="onTagClick"
    data-id="{{item.id}}"
  />
</view>
```

---

## 加载组件

### 基本用法

#### 1. 在页面 JSON 中注册组件

```json
{
  "usingComponents": {
    "zh-loading": "/components/loading/loading"
  }
}
```

#### 2. 在 WXML 中使用

```xml
<zh-loading type="spinner" text="加载中..." />
```

### 属性说明

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | String | 'spinner' | 加载类型: spinner / dots / wave |
| color | String | '#D4B8A5' | 加载动画颜色 |
| size | String | 'medium' | 尺寸: small / medium / large |
| text | String | '' | 加载文字 |
| vertical | Boolean | false | 是否垂直排列 |
| customClass | String | '' | 自定义样式类 |

### 示例

#### 旋转加载

```xml
<zh-loading type="spinner" color="#D4B8A5" text="加载中..." />
```

#### 点状加载

```xml
<zh-loading type="dots" color="#D4B8A5" text="请稍候..." />
```

#### 波浪加载

```xml
<zh-loading type="wave" color="#D4B8A5" />
```

#### 垂直排列

```xml
<zh-loading 
  type="spinner" 
  text="加载中..." 
  vertical="{{true}}"
/>
```

#### 不同尺寸

```xml
<!-- 小尺寸 -->
<zh-loading type="spinner" size="small" />

<!-- 中等尺寸 (默认) -->
<zh-loading type="spinner" size="medium" />

<!-- 大尺寸 -->
<zh-loading type="spinner" size="large" />
```

#### 全屏加载

```xml
<view class="loading-overlay" wx:if="{{isLoading}}">
  <zh-loading 
    type="spinner" 
    color="#FFFFFF" 
    text="加载中..." 
    vertical="{{true}}"
  />
</view>
```

```css
.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}
```

---

## 最佳实践

### 1. 统一使用设计令牌

❌ **不推荐**:
```css
.my-card {
  background: #FFFFFF;
  border-radius: 28rpx;
  padding: 32rpx;
  box-shadow: 0 8rpx 32rpx rgba(212, 184, 165, 0.15);
}
```

✅ **推荐**:
```css
.my-card {
  background: var(--color-bg-card);
  border-radius: var(--radius-2xl);
  padding: var(--spacing-2xl);
  box-shadow: var(--shadow-lg);
}
```

### 2. 优先使用组件

❌ **不推荐**:
```xml
<view class="custom-button" bindtap="onClick">
  <text>点击我</text>
</view>
```

✅ **推荐**:
```xml
<zh-button type="primary" bind:tap="onClick">
  点击我
</zh-button>
```

### 3. 合理使用动画

❌ **不推荐**:
```css
.my-element {
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

✅ **推荐**:
```css
@import "../../styles/animations.wxss";

.my-element {
  animation: fadeIn var(--transition-normal) var(--ease-smooth);
}
```

### 4. 保持视觉一致性

所有卡片使用相同的圆角和阴影：
```xml
<card type="default" shadow="{{true}}">
  <!-- 内容 -->
</card>
```

所有按钮使用统一的尺寸和间距：
```xml
<view class="button-group">
  <zh-button type="secondary" bind:tap="onCancel">取消</zh-button>
  <zh-button type="primary" bind:tap="onConfirm">确认</zh-button>
</view>
```

```css
.button-group {
  display: flex;
  gap: var(--spacing-md);
  padding: var(--spacing-lg);
}
```

### 5. 响应式交互

为所有可点击元素添加交互反馈：
```xml
<view class="clickable-item" hover-class="item-hover" bindtap="onClick">
  <text>点击我</text>
</view>
```

```css
.clickable-item {
  transition: all var(--transition-fast) var(--ease-smooth);
}

.item-hover {
  transform: scale(0.98);
  opacity: 0.8;
}
```

### 6. 性能优化

使用 `will-change` 优化动画性能：
```css
.animated-element {
  will-change: transform, opacity;
  animation: fadeInUp var(--transition-normal) var(--ease-smooth);
}
```

避免过度使用阴影和模糊效果：
```css
/* 适度使用 */
.card {
  box-shadow: var(--shadow-md);
}

/* 避免过度 */
.card-heavy {
  box-shadow: 
    0 0 10rpx rgba(0,0,0,0.1),
    0 0 20rpx rgba(0,0,0,0.1),
    0 0 30rpx rgba(0,0,0,0.1);
  backdrop-filter: blur(20rpx);
}
```

### 7. 组件组合

合理组合组件构建复杂UI：
```xml
<card type="glass" title="情绪记录">
  <view class="emotion-list">
    <emotion-tag 
      wx:for="{{emotions}}" 
      wx:key="id"
      name="{{item.name}}" 
      icon="{{item.icon}}"
      bg-color="{{item.bgColor}}"
      clickable="{{true}}"
      bind:tap="onTagClick"
      data-id="{{item.id}}"
    />
  </view>
  
  <view slot="footer" class="card-actions">
    <zh-button type="secondary" bind:tap="onCancel">
      取消
    </zh-button>
    <zh-button type="primary" bind:tap="onSave">
      保存
    </zh-button>
  </view>
</card>
```

---

## 常见问题

### Q1: 如何自定义组件样式？

使用 `customClass` 属性：

```xml
<zh-button type="primary" customClass="my-custom-button">
  自定义按钮
</zh-button>
```

```css
.my-custom-button {
  width: 200rpx;
  height: 80rpx;
}
```

### Q2: 如何处理组件事件？

在 JS 中定义事件处理函数：

```javascript
Page({
  onButtonClick(e) {
    console.log('按钮被点击', e);
    wx.showToast({
      title: '点击成功',
      icon: 'success'
    });
  }
})
```

### Q3: 如何动态控制组件状态？

使用数据绑定：

```xml
<zh-button 
  type="primary" 
  loading="{{isLoading}}"
  disabled="{{isLoading}}"
  bind:tap="onSubmit">
  {{isLoading ? '提交中...' : '提交'}}
</zh-button>
```

```javascript
Page({
  data: {
    isLoading: false
  },
  
  onSubmit() {
    this.setData({ isLoading: true });
    
    // 模拟异步操作
    setTimeout(() => {
      this.setData({ isLoading: false });
    }, 2000);
  }
})
```

### Q4: 如何在组件中使用设计令牌？

在组件的 `.wxss` 文件中引入设计系统：

```css
@import "../../styles/design-tokens.wxss";

.my-component {
  background: var(--color-bg-card);
  padding: var(--spacing-lg);
  border-radius: var(--radius-xl);
}
```

---

## 更新日志

### v4.0 (2026)
- ✅ 建立统一设计系统
- ✅ 创建按钮组件 (zh-button)
- ✅ 创建卡片组件 (card)
- ✅ 创建情绪标签组件 (emotion-tag)
- ✅ 创建加载组件 (zh-loading)
- ✅ 优化5个核心页面

---

**文档版本**: v4.0  
**更新时间**: 2026年  
**维护者**: Claude Code
