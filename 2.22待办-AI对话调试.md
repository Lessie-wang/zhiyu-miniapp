# 知愈 - 2.22 待办清单：AI 对话风格调试

## 当前状态

- [x] 混元 API 已接通（wx.cloud.extend.AI，hunyuan-turbos-latest）
- [x] DeepSeek 作为备用通道（需充值后可用）
- [x] 降级链路：混元 → DeepSeek → 本地回复

---

## 一、System Prompt 调优

文件：`utils/ai.js` 的 `generateSystemPrompt` 函数（第15行）

当前 prompt 的问题和调整方向：

- [ ] **控制回复长度**：当前要求"50字以内"，实际混元可能不严格遵守，尝试改为"每次回复1-2句话，不超过40字"
- [ ] **调整语气风格**：根据实际回复效果，调整是否太正式/太随意，找到"温暖但不油腻"的平衡点
- [ ] **优化开场白**：第一轮回复（用户发送情绪后）的引导质量，应该自然、不模板化
- [ ] **多轮对话连贯性**：测试3轮以上对话，看 AI 是否能记住上下文、逐步深入
- [ ] **避免说教**：如果 AI 倾向于给建议而不是倾听，在 prompt 中加强"不要给建议，只问问题"

### 调试方法

1. 在小程序中选不同情绪，观察 AI 第一轮回复
2. 连续对话3-5轮，看是否自然
3. 根据效果修改 `generateSystemPrompt` 中的文字
4. 每次改完直接编译测试，不需要部署

---

## 二、模型选择对比

文件：`utils/ai.js` 的 `callHunyuanAPI` 函数（第71行）

当前用的是 `hunyuan-turbos-latest`，可以试试其他模型看效果：

- [ ] 测试 `hunyuan-turbos-latest`（当前）— 速度快，适合对话
- [ ] 测试 `hunyuan-pro`（如果可用）— 质量更高，看是否值得
- [ ] 测试 `hunyuan-lite` — 免费额度更多，看质量是否够用

改模型只需要改两个地方：
```javascript
// 第71行
const model = wx.cloud.extend.AI.createModel('hunyuan-exp');
// 第74行
model: 'hunyuan-turbos-latest',
```

---

## 三、用户画像对 AI 的影响

- [ ] 测试有用户画像 vs 无用户画像时，AI 回复的差异
- [ ] 检查 onboarding 页面收集的画像数据是否正确传到了 AI
- [ ] 如果画像影响不大，考虑在 prompt 中加强画像信息的权重

---

## 四、边界情况测试

- [ ] 用户只发一个字（如"嗯"、"哦"），AI 是否能合理回应
- [ ] 用户发很长的文字，AI 是否能抓住重点
- [ ] 用户选了"不知道"情绪，AI 的引导是否合适
- [ ] 用户同时选多个情绪（如"开心+焦虑"），AI 是否能处理矛盾情绪
- [ ] 网络慢时，"AI 正在思考"的等待体验是否可接受

---

## 五、真机验证

- [ ] 在真机预览中测试完整对话流程
- [ ] 确认混元 API 在真机上也能正常调用（不只是开发工具）
- [ ] 检查微信后台是否需要额外配置（混元走云开发通道，理论上不需要域名白名单）

---

## 快速参考

| 要改什么 | 改哪个文件 | 哪一行 |
|---------|-----------|-------|
| AI 人设/语气 | utils/ai.js | 第16-29行 generateSystemPrompt |
| 模型选择 | utils/ai.js | 第71、74行 callHunyuanAPI |
| 回复最大长度 | utils/ai.js | 第24行 prompt 中的字数限制 |
| 用户画像逻辑 | utils/ai.js | 第32-44行 userProfile 部分 |
| 对话降级逻辑 | utils/ai.js | 第148行 callDeepSeekAPI |
| 本地备用回复 | utils/ai.js | 第174行 generateLocalPrompt |
